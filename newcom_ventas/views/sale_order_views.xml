<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data> 
        <record id="sale_order_view_form" model="ir.ui.view">
            <field name="name">sale.order.view.form.inherit.custom_addon</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="after">
                    <field name="opportunity_id" invisible="1"/>
                </field>

                <xpath expr="//field[@name='analytic_account_id']" position="before">
                    <field name="margen_teorico" invisible="1"/>
                    <field name="margen_teorico_display" groups="account.group_account_manager"/>
                    <field name="margen_teorico_display" groups="!account.group_account_manager" readonly="1"/>
                </xpath>

                <div name="button_box" position="inside">
                    <button name="action_view_opportunity" 
                            type="object"
                            class="oe_stat_button"
                            icon="fa-star"
                            attrs="{'invisible': [('opportunity_id', '=', False)]}">
                        <field name="opportunity_count" widget="statinfo" string="Oportunidad"/>
                    </button>
                </div>

                <button name="action_confirm" position="replace">
                    <button name="action_confirm" id="action_confirm" data-hotkey="v"
                        string="Confirm" class="btn-primary" type="object" context="{'validate_analytic': True}"
                        attrs="{'invisible': [('state', 'not in', ['approved'])]}"/>
                </button>
            </field>
        </record>
    </data>
</odoo>